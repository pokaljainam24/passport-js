<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Verify OTP</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url(/bg-img/christmas-wallpaper-636634_1280.jpg) no-repeat center center/cover;
            backdrop-filter: blur(2px);
        }

        #verifyOTPContainer {
            width: 40%;
            background: rgba(0, 0, 0, 0.7);
            border: 2px solid rgba(255, 255, 255, .2);
            backdrop-filter: blur(4px);
            color: white;
            border-radius: 10px;
            padding: 30px 40px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        .form-label {
            font-weight: bold;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: none;
            text-align: center;
            font-size: 1.5rem;
            letter-spacing: 5px;
        }

        .form-control::placeholder {
            color: white;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            box-shadow: none;
        }

        .btn-primary {
            font-weight: bold;
        }

        a {
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div id="verifyOTPContainer" class="container w-50">
        <h2 class="text-center text-light mb-4">Verify OTP</h2>

        <form id="verifyOTPForm" method="post" action="">
            <div class="mb-3">
                <label for="otp" class="form-label">Enter OTP</label>
                <input type="number" class="form-control text-center" placeholder="6 digit number enter" id="otp"
                    name="otp" maxlength="6" required>
            </div>

            <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
        </form>

        <p class="text-center mt-3">
            Didn't receive OTP? <a href="/forgotPass" class="text-warning">Resend OTP</a>
        </p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Sweetalert CDN -->
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

    <script>
        <% if (flash.success.length) { %>
            swal({
                title: "<%= flash.success %>",
                icon: "success",
                button: "OK",
            })
        <% } %>
        
        <% if (flash.error.length) { %>
            swal({
                title: "<%= flash.error %>",
                icon: "error",
                button: "OK",
            })
        <% } %>    
    </script>

</body>

</html>