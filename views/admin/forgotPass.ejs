<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Forgot Password</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url(/bg-img/christmas-wallpaper-636634_1280.jpg) no-repeat center center/cover;
            backdrop-filter: blur(2px);
        }

        #forgotPasswordContainer {
            width: 40%;
            background: rgba(0, 0, 0, 0.7);
            border: 2px solid rgba(255, 255, 255, .2);
            backdrop-filter: blur(4px);
            color: white;
            border-radius: 10px;
            padding: 30px 40px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        .form-label {
            font-weight: bold;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: none;
        }

        .form-control::placeholder {
            color: white;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            box-shadow: none;
        }

        .btn-primary {
            font-weight: bold;
        }

        a {
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div id="forgotPasswordContainer" class="container w-50">
        <h2 class="text-center text-light mb-4">Forgot Password</h2>

        <form id="forgotPasswordForm" method="post" action="">
            <div class="mb-3">
                <label for="email" class="form-label">Enter Your Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email......📧"
                    required>
            </div>

            <button type="submit" class="btn btn-primary w-100">Send Reset OTP</button>
        </form>

        <p class="text-center mt-3">
            Remembered your password? <a href="/loginForm" class="text-success"
                style="color: greenyellow !important;">Login</a>
        </p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>