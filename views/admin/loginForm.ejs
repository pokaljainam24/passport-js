<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/login.css">
</head>

<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: url(/bg-img/christmas-wallpaper-636634_1280.jpg);
        backdrop-filter: blur(2px);
        background-size: cover;
        background-position: center;
    }

    #main-Container {
        width: 40% !important;
        background: rgba(0, 0, 0, 0.545);
        border: 2px solid rgba(255, 255, 255, .2);
        backdrop-filter: blur(4px);
        color: white;
        border-radius: 10px;
        padding: 30px 40px;
    }
</style>

<body>

    <div id="main-Container" class="container w-50" style="margin-top: 50px;">
        <div class="form-container">
            <h3 class="text-center mb-4">Login</h3>

            <form action="/loginForm" method="post">

                <div class="mb-3">
                    <label for="username" class="form-label">Username</label>
                    <input type="text" class="form-control" id="username" name="username">
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="password">
                </div>

                <button type="submit" class="btn btn-success w-100 mt-3 mb-3">Login</button>

                ---------------------------------------- OR --------------------------------------

                <div class="text-center mt-2">
                    <a href="/forgotPass" class="text-light text-decoration-none fw-bold">Forgot Password?</a>
                </div>
            </form>

            <p class="text-center mt-3">
                Don't have an account? <a href="/registerForm"
                    style="text-decoration: none; color: rgb(76, 127, 255); font-weight: bold;">Sign up</a>
            </p>
        </div>
    </div>


    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

    <% if(flash.success.length){ %>
    <script>
        swal({
            title: "<%= flash.success %>",
            icon: "success",
        });
    </script>
    <% } %>

    <% if(flash.error.length){ %>
        <script>
            swal({
                title: "<%= flash.error %>",
                icon: "error",
            });
        </script>
    <% } %>

</body>

</html>